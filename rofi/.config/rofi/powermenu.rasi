@import "~/.config/rofi/config.rasi"

window {
    width: 400px;
    location: center;
    anchor: center;
}

listview {
    lines: 5;
    columns: 1;
}

element {
    padding: 12px 16px;
    spacing: 10px;
}
