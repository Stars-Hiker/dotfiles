# Defense in depth: nothing in secrets/ is committable EXCEPT the encrypted blob
# and the public recipient. A plaintext secret accidentally dropped here will be
# ignored rather than pushed.
*
!.gitignore
!recipient.txt
!secrets.tar.age
